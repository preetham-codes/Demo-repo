name: first workflow

on: 
  push:
  workflow_dispatch:

jobs:
  Hello-World:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello World"

  # Second job (indented properly under 'jobs')
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: say goodbye
        run: echo "Goodbye" # Added missing closing quote

      # Multiple commands using the pipe (|) symbol
      - name: Multi-line bash
        run: |
          echo "I am"
          echo "Multi Line"
          echo "Script"
